{
  "name": "trackkit-monorepo",
  "packageManager": "pnpm@10.13.1",
  "private": true,
  "scripts": {
    "prebuild": "pnpm -r run prebuild",
    
    "build": "pnpm --filter \"./packages/**\" run build",
    "build:watch": "pnpm --filter \"./packages/**\" run build:watch",
    "build:types": "pnpm --filter \"./packages/**\" run build:types",
    "test": "pnpm --filter \"./packages/**\" run test",
    "test:coverage": "pnpm --filter \"./packages/**\" run test:coverage",
    "typecheck": "pnpm --filter \"./packages/**\" run typecheck",
    "size": "pnpm --filter \"./packages/**\" run size",
    
    "build:examples": "pnpm --filter \"./examples/**\" run build",
    "build:all": "pnpm -r run build",
    "clean": "pnpm -r run clean",
    
    "lint": "eslint . --ext .ts,.tsx --cache",
    "lint:fix": "eslint . --ext .ts,.tsx --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check .",

    "defaults:inject": "pnpm -r run defaults:inject",
    "defaults:assert": "pnpm -r run defaults:assert",
    "test:e2e": "pnpm --filter trackkit run test:e2e",
    "deadcode": "pnpm -r run deadcode",
    "deadcode:ci": "pnpm -r run deadcode:ci",
    "check": "scripts/check.sh",
    "check:quick": "scripts/check.sh --quick",
    "check:docs": "scripts/check.sh --docs",
    "docs:gen:sdk": "pnpm --filter trackkit run typedoc",
    "docs:dev": "vitepress dev docs",
    "docs:preview": "vitepress preview docs",
    "docs:build": "pnpm -s docs:gen:sdk && vitepress build docs"
  },
  "devDependencies": {
    "@eslint/js": "^9.31.0",
    "@typescript-eslint/eslint-plugin": "^7.18.0",
    "@typescript-eslint/parser": "^7.18.0",
    "eslint": "^9.31.0",
    "eslint-config-prettier": "^10.1.5",
    "eslint-plugin-prettier": "^5.5.1",
    "gh-pages": "^6.3.0",
    "globals": "^16.3.0",
    "jsdom": "^26.1.0",
    "knip": "^5.63.1",
    "linkinator": "^7.1.3",
    "msw": "^2.10.4",
    "prettier": "^3.6.2",
    "serve": "^14.2.5",
    "tsup": "^8.5.0",
    "tsx": "^4.20.6",
    "typedoc": "^0.28.13",
    "typedoc-plugin-markdown": "^4.9.0",
    "typescript": "^5.5.4",
    "vitepress": "^1.6.4",
    "vitest": "^3.2.4"
  }
}
